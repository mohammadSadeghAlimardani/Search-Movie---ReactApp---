import "./Navbar.css";

const Navbar = () => {
    return (
        <nav className="navbar">
            <section className="section-center nav-center">
                <h1>SearchMovie</h1>
            </section>
        </nav>
    );
};

export default Navbar;
